# Gradle settings kept portable for different environments.
# If you need to pin a specific JDK locally, set JAVA_HOME in your environment
# or create a local gradle.properties that is not committed to VCS.
